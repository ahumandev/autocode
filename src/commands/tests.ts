export const testsCommandTemplate = `
Generate or improve test coverage for recent code or the specified target.

Use current selection/context when available; if the target is unclear, ask concise clarification.

Detect the test framework and write meaningful tests for happy paths, edge cases, and error cases.

Run focused tests and report exact results.

---

 $ARGUMENTS
`
