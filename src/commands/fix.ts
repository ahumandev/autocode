export const fixCommandTemplate = `
Fix errors, failing tests, lints, stack traces, TODO/comments, or a user-described bug.

Use current selection/context; if the target is unclear, rebuild project and fix errors/tests.

Prefer a minimal focused fix.

Report files changed and exact command results.

---

$ARGUMENTS
`
